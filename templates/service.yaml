apiVersion: v1
kind: Service
metadata:
  name: {{ include "opensearch-cluster.fullname" . }}
  labels:
    {{- include "opensearch-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opensearch-cluster.selectorLabels" . | nindent 4 }}
